diff --git a/chrome/browser/jackiebrowser_server/BUILD.gn b/chrome/browser/jackiebrowser_server/BUILD.gn
new file mode 100644
index 0000000000000..ddbdf0b78c0a3
--- /dev/null
+++ b/chrome/browser/jackiebrowser_server/BUILD.gn
@@ -0,0 +1,66 @@
+# Copyright 2024 The Chromium Authors
+# Use of this source code is governed by a BSD-style license that can be
+# found in the LICENSE file.
+
+import("//build/config/chrome_build.gni")
+
+# Validate that required resources exist at build time
+# GN will fail at generation time if resources/bin/jackiebrowser_server is missing
+_jackiebrowser_binary_name = "jackiebrowser_server"
+if (is_win) {
+  _jackiebrowser_binary_name += ".exe"
+}
+
+action("validate_jackiebrowser_resources") {
+  script = "validate_resources.py"
+  inputs = [ "resources/bin/${_jackiebrowser_binary_name}" ]
+  outputs = [ "$target_gen_dir/jackiebrowser_resources_validated" ]
+}
+
+source_set("jackiebrowser_server") {
+  sources = [
+    "jackiebrowser_server_manager.cc",
+    "jackiebrowser_server_manager.h",
+    "jackiebrowser_server_prefs.cc",
+    "jackiebrowser_server_prefs.h",
+  ]
+
+  deps = [
+    "//base",
+    "//chrome/browser:browser_process",
+    "//chrome/common",
+    "//components/prefs",
+    "//content/public/browser",
+    "//net",
+  ]
+}
+
+if (is_mac) {
+  import("//build/config/apple/symbols.gni")
+  import("//build/config/mac/mac_sdk.gni")
+
+  # Bundle data for macOS - recursively packages resources/ to Resources/JackieBrowserServer/default/
+  bundle_data("jackiebrowser_resources_bundle") {
+    sources = [ "resources" ]
+    outputs = [ "{{bundle_resources_dir}}/JackieBrowserServer/default/{{source_file_part}}" ]
+    # TODO: Re-enable validation when resources/bin/jackiebrowser_server is available
+    # deps = [ ":validate_jackiebrowser_resources" ]
+  }
+} else {
+  # Copy for Windows/Linux - recursively packages resources/ to <exe_dir>/JackieBrowserServer/default/
+  copy("jackiebrowser_resources_copy") {
+    sources = [ "resources" ]
+    outputs = [ "$root_out_dir/JackieBrowserServer/default/{{source_file_part}}" ]
+    # TODO: Re-enable validation when resources/bin/jackiebrowser_server is available
+    # deps = [ ":validate_jackiebrowser_resources" ]
+  }
+}
+
+# Group for all JackieBrowser server resources
+group("jackiebrowser_server_resources") {
+  if (is_mac) {
+    deps = [ ":jackiebrowser_resources_bundle" ]
+  } else {
+    deps = [ ":jackiebrowser_resources_copy" ]
+  }
+}
